# explorenyc-ios
The iOS port of ExploreNYC is a WebKit wrapper of the explorenyc-webpage. This effectively means it is an embedded view of Safari loading up the site, with some extra features. For example, the top and bottom bars adjust to fit light and dark theme. So it has feature parity with the website automatically. It is written in native Swift with Xcode.
